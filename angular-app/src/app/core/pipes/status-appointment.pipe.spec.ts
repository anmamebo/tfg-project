import { StatusAppointmentPipe } from './status-appointment.pipe';

describe('StatusAppointmentPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusAppointmentPipe();
    expect(pipe).toBeTruthy();
  });
});
