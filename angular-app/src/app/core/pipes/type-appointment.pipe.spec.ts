import { TypeAppointmentPipe } from './type-appointment.pipe';

describe('TypeAppointmentPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeAppointmentPipe();
    expect(pipe).toBeTruthy();
  });
});
