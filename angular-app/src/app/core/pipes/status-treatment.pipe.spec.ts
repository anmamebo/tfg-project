import { StatusTreatmentPipe } from './status-treatment.pipe';

describe('StatusTreatmentPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTreatmentPipe();
    expect(pipe).toBeTruthy();
  });
});
